<!-- insert_after "erb:contains('render'):contains('form'):contains(':f') + erb:contains('end')" enabled -->
<%# Replaces the customer billing/shipping address forms with an address selection list %>

<%= render partial: 'spree/admin/addresses/list' %>
